<p hidden id="notice"><%= notice %></p>

<div class="jumbotron">
  <div class="container">
    <h1 class="display-4">Marshall Allshouse's Project Portfolio</h1>
    <hr class="my-4">
    <p>I am a cyber security undergrad at UCCS, here are a few of my projects I have done during my degree program.</p>
    <p hidden class="lead">
      <a class="btn btn-primary btn-lg" href="#" role="button">Link to project I guess??</a>
    </p>
  </div>
</div>

<table class="table table-hover">
  <thead class="table-dark">
    <tr>
      <th>Project Title</th>
      <th>Project Description</th>
      <th>Functions</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @projects.each do |project| %>
      <tr>
        <td><%= project.title %></td>
        <td><%= project.description %></td>
        <td><%= link_to 'Show', project, :class => "btn btn-dark btn-sm"  %>
        <%= link_to 'Edit', edit_project_path(project), :class => "btn btn-dark btn-sm"  %>
        <%= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-danger btn-sm" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<div class="container-fluid">
  <%= link_to 'New Project', new_project_path, :class => "btn btn-success btn-sm"  %>
</div>