<br>

<div class="container">
    <div class="card" style="width: 20rem;">
        <div class="card-body">
            <h3><strong>Editing Project</strong></h3>

            <%= render 'form', project: @project %>

            <%= link_to 'Show', @project, :class => "btn btn-dark btn-sm"  %>
            <%= link_to 'Back', projects_path, :class => "btn btn-dark btn-sm"  %>
        </div>
    </div>
</div>